# Parallel Lives - 平行人生故事生成器

一个基于AI的创意故事生成平台，让用户探索不同人生选择下的平行世界故事。通过个性化测试和AI生成技术，为每个用户创造独特的人生故事体验。

## ✨ 项目特色

- 🎭 **个性化故事生成** - 基于用户输入的人生选择和性格特征生成独特故事
- 🎨 **多种故事风格** - 支持温馨、幽默、浪漫、深沉四种不同的叙述风格
- 🖼️ **AI配图生成** - 自动为故事段落生成配套插图
- 👥 **社区互动** - 用户可以分享故事、点赞评论，构建创意社区
- 📱 **响应式设计** - 完美适配桌面端和移动端设备
- 🔐 **用户系统** - 完整的注册登录、个人资料管理功能

## 🛠️ 技术栈

### 前端
- **React 18** - 现代化前端框架
- **TypeScript** - 类型安全的JavaScript超集
- **Vite** - 快速的前端构建工具
- **Tailwind CSS** - 实用优先的CSS框架
- **React Router** - 客户端路由管理
- **Zustand** - 轻量级状态管理
- **Lucide React** - 现代化图标库
- **Sonner** - 优雅的通知组件

### 后端
- **Node.js** - JavaScript运行时环境
- **Express.js** - Web应用框架
- **TypeScript** - 服务端类型安全
- **Supabase** - 后端即服务平台
  - PostgreSQL数据库
  - 实时数据同步
  - 用户认证
  - 文件存储

### AI服务
- **DeepSeek API** - 故事内容生成
- **图像生成API** - 故事配图生成

### 部署平台
- **Vercel** - 前端部署
- **Zeabur** - 全栈应用部署
- **Supabase** - 数据库和后端服务

## 📁 项目结构

```
parallel-lives/
├── src/                    # 前端源码
│   ├── components/         # React组件
│   │   ├── BackgroundEffects.tsx  # 背景特效组件
│   │   ├── Navbar.tsx             # 导航栏组件
│   │   └── PersonalityTest.tsx    # 性格测试组件
│   ├── pages/              # 页面组件
│   │   ├── Home.tsx        # 首页
│   │   ├── Create.tsx      # 故事创建页
│   │   ├── Stories.tsx     # 故事列表页
│   │   ├── Story.tsx       # 故事详情页
│   │   ├── Profile.tsx     # 个人资料页
│   │   └── Settings.tsx    # 设置页
│   ├── hooks/              # 自定义Hook
│   ├── store/              # 状态管理
│   └── lib/                # 工具库
├── api/                    # 后端API
│   ├── controllers/        # 控制器
│   │   ├── authController.ts      # 用户认证
│   │   ├── storyController.ts     # 故事管理
│   │   ├── commentController.ts   # 评论管理
│   │   └── likeController.ts      # 点赞管理
│   ├── routes/             # 路由定义
│   ├── services/           # 业务服务
│   │   ├── deepseekService.ts     # AI故事生成
│   │   └── imageService.ts        # 图像生成
│   ├── middleware/         # 中间件
│   └── utils/              # 工具函数
├── supabase/               # 数据库
│   └── migrations/         # 数据库迁移文件
└── dist/                   # 构建输出目录
```

## 🚀 快速开始

### 环境要求

- Node.js 18+
- npm 或 pnpm
- Supabase账户

### 安装步骤

1. **克隆项目**
```bash
git clone <repository-url>
cd parallel-lives
```

2. **安装依赖**
```bash
npm install
```

3. **环境配置**

复制环境变量模板：
```bash
cp .env.example .env
```

配置以下环境变量：
```env
# Supabase配置
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# AI服务配置
DEEPSEEK_API_KEY=your_deepseek_api_key

# JWT密钥
JWT_SECRET=your_jwt_secret

# 服务器端口
PORT=3001
```

4. **数据库设置**

在Supabase中创建新项目，然后运行数据库迁移：
```bash
# 应用数据库迁移
npm run db:migrate
```

5. **启动开发服务器**
```bash
# 同时启动前端和后端
npm run dev

# 或分别启动
npm run client:dev  # 前端 (http://localhost:5173)
npm run server:dev  # 后端 (http://localhost:3001)
```

## 📖 使用指南

### 创建故事

1. 注册/登录账户
2. 点击"创建故事"按钮
3. 完成个性化测试问卷
4. 选择故事风格（温馨/幽默/浪漫/深沉）
5. 等待AI生成您的专属平行人生故事
6. 查看生成的故事和配图
7. 选择是否公开分享

### 社区互动

- 浏览其他用户的公开故事
- 为喜欢的故事点赞
- 发表评论与作者互动
- 在个人资料页管理自己的故事

## 🏗️ 构建部署

### 本地构建
```bash
# 构建前端
npm run build

# 预览构建结果
npm run preview

# 类型检查
npm run check
```

### Vercel部署

1. 连接GitHub仓库到Vercel
2. 配置环境变量
3. 部署设置：
   - Build Command: `npm run build`
   - Output Directory: `dist`
   - Install Command: `npm install`

### Zeabur部署

项目已配置`zbpack.json`，支持一键部署：

1. 连接GitHub仓库到Zeabur
2. 配置环境变量
3. 自动构建和部署

## 🗄️ 数据库架构

### 主要数据表

- **users** - 用户信息
- **stories** - 故事内容
- **story_segments** - 故事段落
- **likes** - 点赞记录
- **comments** - 评论数据

### 关键特性

- 完整的用户认证系统
- 故事的多段落结构
- 社交互动功能（点赞、评论）
- 权限控制和数据安全

## 🔧 开发工具

- **ESLint** - 代码质量检查
- **TypeScript** - 类型检查
- **Nodemon** - 后端热重载
- **Concurrently** - 并行运行多个命令
- **Vite** - 前端热重载和快速构建

## 📝 API文档

### 认证接口
- `POST /api/auth/register` - 用户注册
- `POST /api/auth/login` - 用户登录
- `GET /api/auth/profile` - 获取用户信息

### 故事接口
- `POST /api/stories` - 创建故事
- `GET /api/stories` - 获取故事列表
- `GET /api/stories/:id` - 获取故事详情
- `PUT /api/stories/:id` - 更新故事
- `DELETE /api/stories/:id` - 删除故事

### 互动接口
- `POST /api/stories/:id/like` - 点赞/取消点赞
- `POST /api/stories/:id/comments` - 添加评论
- `GET /api/stories/:id/comments` - 获取评论列表

## 🤝 贡献指南

1. Fork 项目
2. 创建功能分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

## 📄 许可证

本项目采用 MIT 许可证 - 查看 [LICENSE](LICENSE) 文件了解详情。

## 🙏 致谢

- [React](https://reactjs.org/) - 前端框架
- [Supabase](https://supabase.com/) - 后端服务
- [Tailwind CSS](https://tailwindcss.com/) - CSS框架
- [Lucide](https://lucide.dev/) - 图标库
- [DeepSeek](https://www.deepseek.com/) - AI服务

---

**Parallel Lives** - 探索无限可能的平行人生 ✨
